/**
 * @deprecated Prefer `npm run proof:money-input-step` (writes PROOF_MONEY_INPUT_STEP_SSOT).
 * Kept as a thin re-run alias so older CI/docs paths still exercise the SSOT.
 */
import './money-input-step.proof.test';
